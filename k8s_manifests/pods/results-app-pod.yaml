apiVersion: v1
kind: Pod
metadata:
  name: result
  labels:
    app: result
# give list of containers to run in the pod
spec:
  containers:
  - name: result
    image: dockersamples/examplevotingapp_result
    ports:
    - containerPort: 80